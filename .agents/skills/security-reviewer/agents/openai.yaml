interface:
  display_name: "Security Reviewer"
  short_description: "Review auth, data, secrets, and server-side security"
  default_prompt: "Use $security-reviewer to review this change for concrete security risks."
